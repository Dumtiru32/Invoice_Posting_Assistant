{
  "name": "ap-validation-backend",
  "version": "1.0.0",
  "description": "Local backend for the AP Invoice Validation Assistant. Handles OCR and AI validation calls.",
  "main": "app.js",
  "scripts": {
    "start": "node app.js"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.19.2"
  }
}
